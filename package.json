{
  "name": "rhythm-extension",
  "version": "1.4.3",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "version": "node scripts/sync-version.js && git add manifest.json",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
